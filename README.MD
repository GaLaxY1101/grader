Plan: 

3. DB schema + Flyway migration
2. Entities + Repositories
3. JWT Auth (login endpoint, filter, SecurityConfig)
4. Assignment CRUD (service + controller + DTOs)
5. Submission service (прийняти файл, зберегти)
6. GitLabApiClient (тільки HTTP клієнт, без логіки)
7. GitLab submission flow (створити репо, залити файли)
8. Webhook handler + log parser
9. Teacher dashboard endpoints